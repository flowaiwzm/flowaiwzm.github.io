### PyTorch相关概念

#### 张量

张量是核心数据结构---包含某种标量类型的n维数据结构（一些数据和一些元数据描述张量的大小和所包含的元素类型和所在设备）

例如元数据中的步幅stride---在计算机中通常在内存中相邻地放置张量的每个元素，为了记住张量的实际维度就必须采用元数据来规定规模大小**步幅表示实际上可以让你表示所有类型的张量域段；**

- 解耦张量的概念（面向用户）和存储张量的数据的实际物理数据的概念（存储storage）
  - 多个张量共享同一存储；存储会定义张量的dtype类型和物理大小；同时张量还会记录大小、步幅和偏移量

